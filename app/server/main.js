import { Meteor } from 'meteor/meteor';
Meteor.startup(() => {
  // Meteor.users.remove({},{justOne: false});
  // AllTasks.remove({},{justOne: false});
  	
  // if (!AllTasks.findOne()) {
  //   var dataIni = [{ name: 'Task1', done: false }, … ];
  //   dataIni.forEach(function(task) { AllTasks.insert(task); });
  //   console.log('All data initialized');
  // }
});